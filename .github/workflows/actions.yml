name: Actions Workflow

on: [push]

jobs:
    run-github-actions:
        runs-on: ubuntu-latest
        steps:
            - name: Simple JS Actions
              uses: actions/hello-world-javacript-action@v1